{
  "name": "claude-window",
  "version": "0.4.2",
  "description": "Pin your Claude Code 5-hour rate-limit window to a fixed hour",
  "license": "MIT",
  "author": "axelhamil",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/axelhamil/claude-window.git"
  },
  "keywords": [
    "claude",
    "claude-code",
    "anthropic",
    "rate-limit",
    "cli"
  ],
  "type": "module",
  "bin": {
    "claude-window": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@11.8.0",
  "scripts": {
    "build": "bun build src/cli.ts --target=node --outfile dist/cli.js --minify",
    "dev": "bun run src/cli.ts",
    "analyze": "bun run scripts/analyze-usage.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "biome check",
    "lint:fix": "biome check --fix",
    "format": "biome format --write",
    "check": "biome check && pnpm typecheck && pnpm test && pnpm build",
    "prepublishOnly": "pnpm build"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.7",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "@types/node": "^22.10.2",
    "@vitest/coverage-v8": "^3.0.0",
    "semantic-release": "^24.2.0",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  }
}
